import React from "react"

const List = () => {
    const list = [1,2,3,4,5,6]

    const changelist = (list) + {
        const tmp = list.map((x)=> x + 2)

        return tmp
    }
}

export default List